const steps = [
  {
    title: "Наведи порядок в основе страницы",
    goal:
      "Сначала убери техническую путаницу в начале страницы и сделай шапку понятной: логотип, навигация и ссылка к рисованию.",
    before:
      "В начале страницы есть лишние и неверные подключения шрифтов, не очень понятный `title`, а ссылка к рисованию оформлена как кнопка внутри ссылки.",
    change:
      "Оставь нормальный `doctype`, `meta viewport`, один CSS-файл, корректные подключения шрифтов и собери шапку из логотипа, навигации и одной ссылки, стилизованной как кнопка.",
    why:
      "Чистая основа помогает странице работать предсказуемо. А ссылка, оформленная как кнопка, проще и правильнее, чем вложенная кнопка внутри ссылки.",
    task:
      "Правь только верх HTML и шапку. Пути к логотипу и странице рисования оставь такими, как уже используются в твоем проекте.",
    recap:
      "Ты навел порядок в базе страницы: убрал неверные подключения, дал странице понятное название и сделал шапку с нормальной ссылкой к редактору.",
    result: {
      location:
        "Где править: начало HTML и блок `header`. Пути в `src` и `href` оставь своими.",
      before: `<title>Document</title>
<link href="https://googleapis.com" rel="stylesheet" />

<header class="header">
  <div class="logo">
    <img src="...путь к логотипу..." alt="" />
  </div>
  <a href="...путь к редактору...">
    <button class="button_draw">...</button>
  </a>
</header>`,
      after: `<title>Pixel Draw - пиксельный редактор</title>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link
  href="https://fonts.googleapis.com/css2?family=Archivo+Black&family=Inter:wght@500;700;800&display=swap"
  rel="stylesheet"
/>

<header class="header">
  <a class="logo" href="#" aria-label="Pixel Draw">
    <img src="...оставь свой путь к логотипу..." alt="Pixel Draw" />
  </a>

  <nav class="nav" aria-label="Навигация по странице">
    <a href="#about">О проекте</a>
    <a href="#gallery">Скриншоты</a>
    <a href="#how">Как работает</a>
  </nav>

  <a class="header-button" href="...оставь свой путь к редактору...">Рисовать</a>
</header>`,
      note:
        "Ссылка может выглядеть как кнопка через CSS. Так HTML остается проще: один интерактивный элемент вместо ссылки, внутри которой лежит кнопка.",
    },
    hints: [
      {
        level: "Маленькая подсказка",
        difficulty: "Простой вопрос",
        question: "Что лучше использовать для перехода на страницу рисования?",
        answer: "link",
        options: [
          {
            value: "button-inside-link",
            label: "Кнопку внутри ссылки",
            explain:
              "Нет. Это лишнее вложение интерактивных элементов, его лучше убрать.",
          },
          {
            value: "link",
            label: "Обычную ссылку, оформленную как кнопку",
            explain:
              "Верно. Ссылка отвечает за переход, а CSS делает ее похожей на кнопку.",
          },
          {
            value: "only-button",
            label: "Только кнопку без `href`",
            explain:
              "Нет. Кнопка сама по себе не ведет на другую страницу без дополнительного JS.",
          },
        ],
        body:
          "<p>Если элемент должен вести на другую страницу, используй <code>a</code>. Чтобы он выглядел как кнопка, дай ему класс и оформи в CSS.</p>",
      },
      {
        level: "Средняя подсказка",
        difficulty: "Вопрос чуть точнее",
        question: "Зачем шапке нужен `nav`?",
        answer: "navigation",
        options: [
          {
            value: "decoration",
            label: "Только для декоративной рамки",
            explain: "Нет. `nav` нужен не для рамки, а для навигации.",
          },
          {
            value: "navigation",
            label: "Чтобы собрать ссылки по разделам страницы",
            explain:
              "Верно. В `nav` удобно хранить ссылки на основные секции: проект, скриншоты и принцип работы.",
          },
          {
            value: "image",
            label: "Чтобы вставить логотип",
            explain: "Нет. Логотип можно оставить отдельной ссылкой или блоком.",
          },
        ],
        body:
          "<p>Навигация помогает посетителю быстро понять, что есть на странице. Даже если сайт небольшой, ссылки в шапке делают его собраннее.</p>",
      },
      {
        level: "Почти готовое решение",
        difficulty: "Точный вопрос",
        question: "Что нужно сделать с путями к логотипу и редактору?",
        answer: "keep",
        options: [
          {
            value: "keep",
            label: "Оставить существующие пути проекта",
            explain:
              "Верно. Мы улучшаем структуру и оформление, но не ломаем рабочие пути.",
          },
          {
            value: "random",
            label: "Заменить на случайные новые пути",
            explain:
              "Нет. Тогда картинка или переход могут перестать работать.",
          },
          {
            value: "delete",
            label: "Удалить `src` и `href`",
            explain:
              "Нет. Без этих атрибутов логотип и переход к рисованию потеряют смысл.",
          },
        ],
        body:
          "<p>В коде можно поменять классы и структуру, но значения <code>src</code> у логотипа и <code>href</code> у ссылки к редактору оставь теми, которые уже работают в твоем проекте.</p>",
      },
    ],
  },
  {
    title: "Собери сильный первый экран",
    goal:
      "Главная страница должна сразу объяснять, что это пиксельный редактор, и вести к рисованию большой заметной кнопкой.",
    before:
      "После шапки сразу идет длинный текст. Из-за этого посетитель не видит короткого объяснения проекта и главного действия.",
    change:
      "Добавь первый экран: заголовок, короткое описание, большую кнопку `Перейти к рисованию` и визуальный блок с пиксельной сеткой.",
    why:
      "Первый экран отвечает на три вопроса: что это, зачем это нужно и куда нажать, чтобы начать.",
    task:
      "Добавь секцию после шапки. Не удаляй длинный текст в `pre`: он понадобится на следующем шаге.",
    recap:
      "Ты сделал страницу понятнее с первых секунд: теперь есть заголовок, краткое описание, большой переход к редактору и визуальный намек на пиксельную сетку.",
    result: {
      location: "Где править: внутри `main`, первым крупным блоком после шапки.",
      before: `<div class="txt-holder">
  <pre>
    длинный текст о проекте
  </pre>
</div>`,
      after: `<main>
  <section class="hero-section" id="about">
    <div class="hero-text">
      <p class="eyebrow">Pixel editor</p>
      <h1>Пиксельный редактор прямо в браузере</h1>
      <p class="hero-lead">
        Выбирай цвет, закрашивай клетки, очищай поле, заливай сетку
        и сохраняй рисунок.
      </p>
      <a class="draw-button" href="...оставь свой путь к редактору...">
        Перейти к рисованию
      </a>
    </div>

    <div class="hero-preview" aria-label="Пример пиксельного рисунка">
      <div class="pixel-board" aria-hidden="true">
        <span></span><span></span><span></span><span></span><span></span>
        <span></span><span></span><span></span><span></span><span></span>
        <span></span><span></span><span></span><span></span><span></span>
        <span></span><span></span><span></span><span></span><span></span>
        <span></span><span></span><span></span><span></span><span></span>
      </div>
    </div>
  </section>

  <!-- блок с pre будет ниже -->
</main>`,
      note:
        "Большая кнопка должна быть именно на первом экране. Маленькая ссылка в шапке полезна, но она не заменяет главный призыв к действию.",
    },
    hints: [
      {
        level: "Маленькая подсказка",
        difficulty: "Простой вопрос",
        question: "Какой главный текст должен быть на первом экране?",
        answer: "project-title",
        options: [
          {
            value: "project-title",
            label: "Короткий заголовок о пиксельном редакторе",
            explain:
              "Верно. Посетитель должен сразу понять, что перед ним редактор для пиксельного рисования.",
          },
          {
            value: "copyright",
            label: "Только копирайт",
            explain:
              "Нет. Копирайт нужен внизу, а не как главный смысл страницы.",
          },
          {
            value: "random-text",
            label: "Случайный длинный текст",
            explain:
              "Нет. Первый экран должен объяснять проект быстро и понятно.",
          },
        ],
        body:
          "<p>Начни с короткого заголовка. Он должен сказать не “Document” и не “Welcome”, а прямо объяснить: это пиксельный редактор.</p>",
      },
      {
        level: "Средняя подсказка",
        difficulty: "Вопрос чуть точнее",
        question: "Где лучше разместить большую кнопку `Перейти к рисованию`?",
        answer: "hero",
        options: [
          {
            value: "hero",
            label: "На первом экране рядом с описанием",
            explain:
              "Верно. Это главное действие, оно должно быть видно сразу.",
          },
          {
            value: "only-footer",
            label: "Только в подвале",
            explain:
              "Нет. Если кнопка только внизу, посетитель может до нее не дойти.",
          },
          {
            value: "hidden",
            label: "Спрятать в комментарий HTML",
            explain:
              "Нет. Главная кнопка должна быть видимой и рабочей.",
          },
        ],
        body:
          "<p>Кнопка к рисованию — главная цель страницы. Поэтому ее нужно поставить рядом с заголовком и описанием, а не прятать внизу.</p>",
      },
      {
        level: "Почти готовое решение",
        difficulty: "Точный вопрос",
        question: "Зачем нужен визуальный блок с пиксельной сеткой на первом экране?",
        answer: "visual",
        options: [
          {
            value: "visual",
            label: "Чтобы сразу показать идею редактора",
            explain:
              "Верно. Даже простая сетка помогает понять, что проект связан с пиксель-артом.",
          },
          {
            value: "replace-button",
            label: "Чтобы заменить кнопку перехода",
            explain:
              "Нет. Визуальный блок дополняет кнопку, но не заменяет ее.",
          },
          {
            value: "hide-text",
            label: "Чтобы убрать описание проекта",
            explain:
              "Нет. Визуал и текст должны работать вместе.",
          },
        ],
        body:
          "<p>Сделай справа простой блок <code>.pixel-board</code> из маленьких <code>span</code>. Его можно оформить через CSS как пиксельную картинку, без сложного canvas и без JS.</p>",
      },
    ],
  },
  {
    title: "Сохрани текстовый блок в `pre`",
    goal:
      "Длинный текст о проекте должен остаться в `pre`, потому что там важны переносы, отступы и общий вид.",
    before:
      "Текст уже находится в `pre`, но вокруг него мало оформления, а символы и содержание лучше привести к читаемому виду.",
    change:
      "Оберни `pre` в отдельный блок, обнови текст о проекте и оформи `pre` так, чтобы переносы сохранялись, а на маленьком экране появлялась прокрутка.",
    why:
      "`pre` сохраняет пробелы и переносы. Если заменить его на обычный `p`, формат текста изменится.",
    task:
      "Не меняй `pre` на обычный текстовый блок. Улучши содержимое и оформление, но сохрани сам принцип.",
    recap:
      "Ты сохранил важное поведение `pre`: текст остается форматированным, но теперь выглядит аккуратно и не ломает страницу.",
    result: {
      location:
        "Где править: HTML-блок с длинным текстом и CSS-стили для `.intro-card pre`.",
      before: `<div class="txt-holder">
  <pre>
    PIXEL ... длинный текст ...
  </pre>
</div>`,
      after: `<section class="intro-card">
  <pre>
PIXEL - Draw Your Imagination One Pixel at a Time

Welcome to PIXEL, a simple and fun place where you can create pixel art directly in your browser.
Use the grid to draw icons, characters, game sprites, patterns and small experiments.

Features:

- easy-to-use pixel drawing grid
- multiple colors to choose from
- fill and clear tools for faster drawing
- saving the result as an image
- perfect for beginners and pixel art fans

Start drawing now and turn tiny squares into awesome art.
  </pre>
</section>`,
      note:
        "У `pre` пробелы и переносы являются частью внешнего вида. Поэтому его нужно оформлять аккуратно, а не заменять на несколько обычных абзацев.",
    },
    hints: [
      {
        level: "Маленькая подсказка",
        difficulty: "Простой вопрос",
        question: "Почему нельзя просто заменить `pre` на обычный `p`?",
        answer: "keeps-format",
        options: [
          {
            value: "keeps-format",
            label: "`pre` сохраняет переносы и пробелы",
            explain:
              "Верно. Именно поэтому этот текст должен остаться в `pre`.",
          },
          {
            value: "faster",
            label: "`pre` всегда быстрее загружает страницу",
            explain:
              "Нет. Скорость здесь не главная причина.",
          },
          {
            value: "image",
            label: "`pre` автоматически делает скриншоты",
            explain:
              "Нет. `pre` отвечает только за отображение текста.",
          },
        ],
        body:
          "<p><code>pre</code> полезен, когда переносы строк и пробелы важны для внешнего вида. Поэтому оставь этот тег и работай с его содержимым и стилями.</p>",
      },
      {
        level: "Средняя подсказка",
        difficulty: "Вопрос чуть точнее",
        question: "Что нужно сделать, чтобы широкий `pre` не ломал мобильную верстку?",
        answer: "overflow",
        options: [
          {
            value: "overflow",
            label: "Добавить горизонтальную прокрутку",
            explain:
              "Верно. Для `pre` это нормальное решение, потому что переносы нужно сохранить.",
          },
          {
            value: "delete",
            label: "Удалить весь длинный текст",
            explain:
              "Нет. Текст нужен, просто его нужно правильно оформить.",
          },
          {
            value: "absolute",
            label: "Поставить `position: absolute`",
            explain:
              "Нет. Это не решит проблему ширины и может сломать поток страницы.",
          },
        ],
        body:
          "<p>Для контейнера можно поставить <code>overflow: auto</code>, а у самого <code>pre</code> оставить <code>white-space: pre</code>. Так формат сохранится, а страница не разъедется.</p>",
      },
      {
        level: "Почти готовое решение",
        difficulty: "Точный вопрос",
        question: "Какое CSS-свойство явно сохраняет форматирование `pre`?",
        answer: "white-space",
        options: [
          {
            value: "white-space",
            label: "`white-space: pre`",
            explain:
              "Верно. Это свойство сохраняет пробелы и переносы в тексте.",
          },
          {
            value: "display-grid",
            label: "`display: grid`",
            explain:
              "Нет. Grid отвечает за раскладку элементов, а не за переносы текста.",
          },
          {
            value: "font-size",
            label: "`font-size`",
            explain:
              "Нет. Размер шрифта важен, но он не отвечает за сохранение переносов.",
          },
        ],
        body:
          '<p>Добавь CSS:</p><pre><code>.intro-card {\n  overflow: auto;\n}\n\n.intro-card pre {\n  min-width: 720px;\n  margin: 0;\n  padding: 24px;\n  font-family: "Archivo Black", Arial, sans-serif;\n  font-size: 0.92rem;\n  line-height: 1.7;\n  white-space: pre;\n}</code></pre><p>Так блок выглядит аккуратно, но ведет себя как настоящий <code>pre</code>.</p>',
      },
    ],
  },
  {
    title: "Добавь скриншоты редактора",
    goal:
      "Главная страница должна не только рассказывать, но и показывать, как выглядит редактор.",
    before:
      "На странице много свободного места, но нет визуальных примеров работы редактора.",
    change:
      "Добавь секцию со скриншотами: рабочая сетка, палитра инструментов и готовый рисунок. У каждого изображения должна быть подпись.",
    why:
      "Скриншоты помогают быстрее понять проект. Посетитель видит не абстрактное описание, а реальные части редактора.",
    task:
      "Добавь секцию после блока `pre`. Если настоящих скриншотов еще нет, сделай простые SVG-изображения в папке с картинками сайта.",
    recap:
      "Ты заполнил пустое место полезным контентом: теперь на странице есть визуальные примеры редактора и подписи к ним.",
    result: {
      location:
        "Где править: HTML после `pre` и папка с картинками сайта. Пути в `src` укажи под свой проект.",
      before: `<!-- скриншотов редактора на странице пока нет -->`,
      after: `<section class="section" id="gallery">
  <div class="section-head">
    <p class="eyebrow">Screens</p>
    <h2>Скриншоты редактора</h2>
    <p>
      Эти изображения показывают основные части редактора: рабочую сетку,
      панель инструментов и готовый рисунок.
    </p>
  </div>

  <div class="shots">
    <article class="shot-card">
      <img class="shot-image" src="...путь к скриншоту сетки..." alt="Рабочая сетка редактора" />
      <h3>Рабочая сетка</h3>
      <p>Главная область редактора, где каждая клетка становится частью рисунка.</p>
    </article>

    <article class="shot-card">
      <img class="shot-image" src="...путь к скриншоту инструментов..." alt="Панель цветов и инструментов" />
      <h3>Палитра цветов</h3>
      <p>Кнопки цветов помогают быстро переключаться между оттенками.</p>
    </article>

    <article class="shot-card">
      <img class="shot-image" src="...путь к скриншоту результата..." alt="Готовый пиксельный рисунок" />
      <h3>Готовый рисунок</h3>
      <p>После работы изображение можно сохранить и использовать отдельно.</p>
    </article>
  </div>
</section>`,
      note:
        "Даже если скриншоты сделаны как простые SVG-картинки, они должны выглядеть как часть проекта: сетка, цвета, рамки и пиксельный стиль.",
    },
    hints: [
      {
        level: "Маленькая подсказка",
        difficulty: "Простой вопрос",
        question: "Зачем на главной странице нужны скриншоты редактора?",
        answer: "show-product",
        options: [
          {
            value: "show-product",
            label: "Чтобы показать, как выглядит проект",
            explain:
              "Верно. Скриншоты помогают понять редактор быстрее, чем один текст.",
          },
          {
            value: "hide-content",
            label: "Чтобы спрятать описание проекта",
            explain:
              "Нет. Скриншоты дополняют текст, а не заменяют его полностью.",
          },
          {
            value: "break-layout",
            label: "Чтобы занять место любыми картинками",
            explain:
              "Нет. Изображения должны быть связаны именно с редактором.",
          },
        ],
        body:
          "<p>Покажи три состояния: поле рисования, инструменты и готовый результат. Так страница станет информативной, а не пустой.</p>",
      },
      {
        level: "Средняя подсказка",
        difficulty: "Вопрос чуть точнее",
        question: "Что обязательно нужно добавить каждому изображению?",
        answer: "alt",
        options: [
          {
            value: "alt",
            label: "Понятный `alt`",
            explain:
              "Верно. `alt` объясняет изображение, если оно не загрузилось или страницу читает скринридер.",
          },
          {
            value: "empty-src",
            label: "Пустой `src`",
            explain:
              "Нет. Без `src` изображение не загрузится.",
          },
          {
            value: "random-id",
            label: "Случайный `id`",
            explain:
              "Нет. Для этих карточек `id` не обязателен.",
          },
        ],
        body:
          "<p>У каждого <code>img</code> должен быть рабочий <code>src</code> и понятный <code>alt</code>. Например: “Рабочая сетка редактора”.</p>",
      },
      {
        level: "Почти готовое решение",
        difficulty: "Точный вопрос",
        question: "Как лучше расположить три карточки со скриншотами на широком экране?",
        answer: "grid",
        options: [
          {
            value: "grid",
            label: "Через сетку из трех колонок",
            explain:
              "Верно. Три карточки удобно поставить в одну строку через CSS Grid.",
          },
          {
            value: "absolute",
            label: "Через `position: absolute`",
            explain:
              "Нет. Абсолютное позиционирование здесь усложнит адаптивность.",
          },
          {
            value: "one-line-text",
            label: "Оставить только текст без картинок",
            explain:
              "Нет. В этом шаге важно добавить именно визуальные примеры.",
          },
        ],
        body:
          '<p>Для CSS используй простую сетку:</p><pre><code>.shots {\n  display: grid;\n  grid-template-columns: repeat(3, minmax(0, 1fr));\n  gap: 18px;\n}\n\n.shot-image {\n  width: 100%;\n  aspect-ratio: 16 / 10;\n  border: 2px solid black;\n  object-fit: cover;\n  display: block;\n}</code></pre><p>На мобильной ширине эту сетку потом нужно перестроить в одну колонку.</p>',
      },
    ],
  },
  {
    title: "Объясни принцип работы редактора",
    goal:
      "Добавь раздел, который простыми словами объясняет, как устроено рисование.",
    before:
      "На странице есть описание возможностей, но нет понятного объяснения: из чего состоит редактор и как он работает.",
    change:
      "Добавь секцию с тремя карточками: сетка из клеток, выбор активного цвета, инструменты для очистки, заливки и сохранения.",
    why:
      "Так страница становится не просто красивой, а обучающей: посетитель понимает логику редактора до перехода к рисованию.",
    task:
      "Добавь секцию после скриншотов. Пиши коротко: одна карточка - одна идея.",
    recap:
      "Ты добавил смысловой блок: теперь главная страница объясняет не только внешний вид редактора, но и принцип его работы.",
    result: {
      location: "Где править: HTML после секции скриншотов.",
      before: `<!-- объяснения принципов работы пока нет -->`,
      after: `<section class="section section-split" id="how">
  <div>
    <p class="eyebrow">How it works</p>
    <h2>Принцип работы редактора</h2>
  </div>

  <div class="steps">
    <article class="step">
      <span>1</span>
      <h3>Сетка создается из клеток</h3>
      <p>JavaScript добавляет маленькие блоки. Каждый блок можно закрасить выбранным цветом.</p>
    </article>

    <article class="step">
      <span>2</span>
      <h3>Палитра выбирает активный цвет</h3>
      <p>Когда ты нажимаешь на цвет, редактор запоминает его и применяет к следующим клеткам.</p>
    </article>

    <article class="step">
      <span>3</span>
      <h3>Инструменты ускоряют работу</h3>
      <p>Очистка, заливка и сохранение помогают быстро получить готовый результат.</p>
    </article>
  </div>
</section>`,
      note:
        "Этот блок не должен быть длинным. Его задача - быстро объяснить принцип: клетка, цвет, действие.",
    },
    hints: [
      {
        level: "Маленькая подсказка",
        difficulty: "Простой вопрос",
        question: "С чего начинается объяснение работы пиксельного редактора?",
        answer: "grid",
        options: [
          {
            value: "grid",
            label: "С сетки из клеток",
            explain:
              "Верно. Пиксельный рисунок строится из маленьких клеток.",
          },
          {
            value: "footer",
            label: "С подвала сайта",
            explain:
              "Нет. Подвал не объясняет принцип рисования.",
          },
          {
            value: "random-design",
            label: "Со случайных украшений",
            explain:
              "Нет. Важно объяснить именно редактор.",
          },
        ],
        body:
          "<p>Начни с сетки. Это основа пиксельного редактора: пользователь закрашивает маленькие клетки и получает рисунок.</p>",
      },
      {
        level: "Средняя подсказка",
        difficulty: "Вопрос чуть точнее",
        question: "Почему карточки лучше делать короткими?",
        answer: "scan",
        options: [
          {
            value: "scan",
            label: "Их легче быстро прочитать",
            explain:
              "Верно. Карточки должны объяснять идею быстро, без больших полотен текста.",
          },
          {
            value: "harder",
            label: "Так сложнее понять проект",
            explain:
              "Нет. Цель обратная: сделать объяснение проще.",
          },
          {
            value: "no-css",
            label: "Чтобы не писать CSS",
            explain:
              "Нет. CSS все равно понадобится для аккуратной сетки карточек.",
          },
        ],
        body:
          "<p>Одна карточка — одна мысль. Например: “сетка”, “палитра”, “инструменты”. Так блок легко читать.</p>",
      },
      {
        level: "Почти готовое решение",
        difficulty: "Точный вопрос",
        question: "Какие три идеи лучше объяснить в этом разделе?",
        answer: "three",
        options: [
          {
            value: "three",
            label: "Сетка, палитра, инструменты",
            explain:
              "Верно. Это три главные части работы редактора.",
          },
          {
            value: "only-colors",
            label: "Только список цветов",
            explain:
              "Нет. Цвета важны, но это не вся логика редактора.",
          },
          {
            value: "only-history",
            label: "Только история пиксель-арта",
            explain:
              "Нет. Страница должна объяснять именно твой проект.",
          },
        ],
        body:
          "<p>Собери три карточки: первая про клетки, вторая про активный цвет, третья про действия вроде очистки, заливки и сохранения.</p>",
      },
    ],
  },
  {
    title: "Перенеси общий стиль в CSS",
    goal:
      "Теперь оформи страницу так, чтобы она осталась в стиле проекта: зеленая шапка, черные рамки, простые блоки и пиксельное настроение.",
    before:
      "В CSS есть отдельные правила, но часть из них не работает как задумано, а страница держится на высоте `100vh` и оставляет много пустого места.",
    change:
      "Добавь базовые переменные, нормальный фон, общий контейнер, стили шапки, hero-блока, кнопок, карточек, секций и подвала.",
    why:
      "Когда стили собраны системно, страница выглядит цельной и не требует случайных отступов и фиксированных высот.",
    task:
      "Перепиши CSS блоками: база, шапка, первый экран, текстовый блок, скриншоты, объяснение, подвал.",
    recap:
      "Ты перевел внешний вид страницы из набора разрозненных правил в цельную систему: блоки выровнены, пустота заполнена, стиль остался узнаваемым.",
    result: {
      location: "Где править: CSS-файл страницы. Начни с базовых переменных и общих правил.",
      before: `body {
  display: flex;
  justify-content: space-between;
}

.container {
  height: 100vh;
}

.txt-holder {
  width: fit-content;
  height: fit-content;
}`,
      after: `:root {
  --green: rgb(31, 185, 95);
  --green-soft: rgba(31, 185, 95, 0.18);
  --black: #050505;
  --white: #ffffff;
  --paper: #fbfff8;
  --muted: #4f5a55;
  --blue: darkblue;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  color: var(--black);
  background:
    linear-gradient(rgba(31, 185, 95, 0.06) 1px, transparent 1px),
    linear-gradient(90deg, rgba(31, 185, 95, 0.06) 1px, transparent 1px),
    var(--paper);
  background-size: 28px 28px;
  font-family: Inter, Arial, sans-serif;
}

main {
  width: min(1180px, calc(100% - 32px));
  margin: 0 auto;
}

.section,
.intro-card,
.hero-preview {
  border: 2px solid var(--black);
  border-radius: 8px;
  background: var(--white);
  box-shadow: 8px 8px 0 var(--black);
}`,
      note:
        "Главная идея: меньше случайных фиксированных размеров, больше понятных секций, общих переменных и повторяемого стиля.",
    },
    hints: [
      {
        level: "Маленькая подсказка",
        difficulty: "Простой вопрос",
        question: "Зачем в CSS нужны переменные в `:root`?",
        answer: "reuse",
        options: [
          {
            value: "reuse",
            label: "Чтобы переиспользовать цвета и значения",
            explain:
              "Верно. Переменные помогают держать стиль страницы цельным.",
          },
          {
            value: "hide",
            label: "Чтобы спрятать HTML",
            explain:
              "Нет. CSS-переменные не прячут HTML.",
          },
          {
            value: "replace-js",
            label: "Чтобы заменить JavaScript",
            explain:
              "Нет. Переменные CSS отвечают за оформление, а не за логику.",
          },
        ],
        body:
          "<p>Если зеленый цвет, черная рамка и белый фон повторяются много раз, вынеси их в переменные. Потом стиль проще поддерживать.</p>",
      },
      {
        level: "Средняя подсказка",
        difficulty: "Вопрос чуть точнее",
        question: "Почему лучше убрать зависимость всей страницы от `height: 100vh`?",
        answer: "content",
        options: [
          {
            value: "content",
            label: "Страница должна расти по содержимому",
            explain:
              "Верно. На главной много секций, поэтому высота должна зависеть от контента.",
          },
          {
            value: "hide-footer",
            label: "Чтобы спрятать подвал",
            explain:
              "Нет. Подвал должен остаться видимым в конце страницы.",
          },
          {
            value: "no-scroll",
            label: "Чтобы запретить прокрутку",
            explain:
              "Нет. Главная страница с несколькими секциями нормально прокручивается.",
          },
        ],
        body:
          "<p><code>height: 100vh</code> подходит для одного экрана, но мешает странице с несколькими секциями. Используй нормальный поток документа и отступы между блоками.</p>",
      },
      {
        level: "Почти готовое решение",
        difficulty: "Точный вопрос",
        question: "Какие элементы должны повторять общий стиль с рамкой и тенью?",
        answer: "blocks",
        options: [
          {
            value: "blocks",
            label: "Секции, карточки, превью и текстовый блок",
            explain:
              "Верно. Эти элементы должны выглядеть частью одной системы.",
          },
          {
            value: "only-body",
            label: "Только `body`",
            explain:
              "Нет. `body` задает основу, но блокам тоже нужны свои рамки и фон.",
          },
          {
            value: "only-footer",
            label: "Только подвал",
            explain:
              "Нет. Подвал важен, но стиль должен повторяться по всей странице.",
          },
        ],
        body:
          "<p>Сделай общий визуальный язык: белый фон, черная рамка, небольшой радиус, пиксельная простота и тень. Так страница будет выглядеть собранной.</p>",
      },
    ],
  },
  {
    title: "Доделай адаптивность и подвал",
    goal:
      "Финальный шаг: проверь, что страница нормально смотрится на маленьком экране, а подвал закрывает сайт аккуратно.",
    before:
      "Без адаптивных правил сетки из колонок могут стать слишком узкими, а широкий `pre` может вылезти за экран.",
    change:
      "Добавь медиазапросы: на планшетах перестрой hero и секции в одну колонку, на телефонах спрячь лишнюю навигацию, растяни большую кнопку и оставь прокрутку у `pre`.",
    why:
      "Главная страница должна быть удобной не только на большом мониторе, но и на телефоне.",
    task:
      "Проверь ширины примерно 900px и 560px. Все блоки должны идти сверху вниз и не создавать горизонтального скролла у всей страницы.",
    recap:
      "Ты закончил доработку: страница стала цельной, понятной, адаптивной и сохранила стиль пиксельного редактора.",
    result: {
      location:
        "Где править: конец CSS и небольшой JS-файл, если хочешь автоматически обновлять год в подвале.",
      before: `/* адаптивных правил почти нет */`,
      after: `.footer {
  min-height: 96px;
  padding: 22px clamp(16px, 4vw, 56px);
  border-top: 2px solid var(--blue);
  background-color: var(--blue);
  color: var(--white);
  display: flex;
  justify-content: space-between;
  gap: 16px;
  align-items: center;
  flex-wrap: wrap;
}

@media (max-width: 900px) {
  .header,
  .hero-section,
  .section-split,
  .shots {
    grid-template-columns: 1fr;
  }

  .hero-section {
    min-height: auto;
  }
}

@media (max-width: 560px) {
  .nav {
    display: none;
  }

  .draw-button {
    width: 100%;
  }

  .intro-card pre {
    min-width: 600px;
    font-size: 0.82rem;
  }
}`,
      note:
        "Горизонтальная прокрутка допустима внутри блока `pre`, потому что так сохраняется формат текста. Но вся страница целиком не должна разъезжаться.",
    },
    hints: [
      {
        level: "Маленькая подсказка",
        difficulty: "Простой вопрос",
        question: "Что должно происходить с колонками на маленьком экране?",
        answer: "one-column",
        options: [
          {
            value: "one-column",
            label: "Они должны становиться одной колонкой",
            explain:
              "Верно. На узком экране блоки удобнее читать сверху вниз.",
          },
          {
            value: "five-columns",
            label: "Они должны становиться пятью колонками",
            explain:
              "Нет. На телефоне для пяти колонок слишком мало места.",
          },
          {
            value: "absolute",
            label: "Их нужно наложить друг на друга",
            explain:
              "Нет. Наложение ухудшит читаемость.",
          },
        ],
        body:
          "<p>На мобильной ширине сетки должны превращаться в одну колонку. Это касается первого экрана, скриншотов и блока объяснения.</p>",
      },
      {
        level: "Средняя подсказка",
        difficulty: "Вопрос чуть точнее",
        question: "Почему навигацию в шапке можно скрыть на очень узком экране?",
        answer: "space",
        options: [
          {
            value: "space",
            label: "Чтобы освободить место для логотипа и кнопки",
            explain:
              "Верно. На маленьком экране важнее оставить главный переход к рисованию.",
          },
          {
            value: "delete-links",
            label: "Чтобы навигация исчезла из HTML",
            explain:
              "Нет. CSS скрывает ее визуально, HTML при этом остается.",
          },
          {
            value: "break",
            label: "Чтобы сломать переходы по странице",
            explain:
              "Нет. Мы скрываем только лишний визуальный блок на узкой ширине.",
          },
        ],
        body:
          "<p>Если шапка не помещается, оставь логотип и кнопку рисования. Навигационные ссылки можно скрыть через <code>display: none</code> только на маленькой ширине.</p>",
      },
      {
        level: "Почти готовое решение",
        difficulty: "Точный вопрос",
        question: "Где допустима горизонтальная прокрутка после доработки?",
        answer: "pre",
        options: [
          {
            value: "pre",
            label: "Только внутри блока `pre`",
            explain:
              "Верно. Так форматированный текст сохраняет переносы и не ломает всю страницу.",
          },
          {
            value: "whole-page",
            label: "У всей страницы целиком",
            explain:
              "Нет. Горизонтальный скролл всей страницы обычно выглядит как ошибка.",
          },
          {
            value: "button",
            label: "Только внутри кнопки",
            explain:
              "Нет. Текст кнопки должен помещаться без прокрутки.",
          },
        ],
        body:
          "<p>Проверь в браузере: страница не должна ехать вбок. Если прокрутка появляется только внутри <code>pre</code>, это нормально для форматированного текста.</p>",
      },
    ],
  },
];

const penaltyQuestionBank = [
  {
    question: "Какой тег в HTML создает ссылку?",
    answer: "a",
    options: [
      { value: "a", label: "`<a>`", explain: "Верно. Ссылки в HTML создаются тегом `<a>`." },
      { value: "button", label: "`<button>`", explain: "Нет. Кнопка запускает действие, но сама по себе не является ссылкой." },
      { value: "img", label: "`<img>`", explain: "Нет. `<img>` вставляет изображение." },
      { value: "section", label: "`<section>`", explain: "Нет. `<section>` задает смысловой раздел страницы." },
      { value: "pre", label: "`<pre>`", explain: "Нет. `<pre>` нужен для форматированного текста." },
    ],
    body: "<p>Для перехода на другую страницу обычно используют <code>&lt;a href=\"...\"&gt;</code>.</p>",
  },
  {
    question: "Как в CSS обратиться к классу `hero`?",
    answer: "class",
    options: [
      { value: "class", label: "`.hero`", explain: "Верно. Точка означает селектор по классу." },
      { value: "id", label: "`#hero`", explain: "Нет. Решетка означает селектор по `id`." },
      { value: "tag", label: "`hero`", explain: "Нет. Так CSS искал бы тег `<hero>`." },
      { value: "attr", label: "`[hero]`", explain: "Нет. Это селектор по атрибуту." },
      { value: "pseudo", label: "`:hero`", explain: "Нет. Двоеточие используют для псевдоклассов и псевдоэлементов." },
    ],
    body: "<p>Если в HTML написано <code>class=\"hero\"</code>, то в CSS пишут <code>.hero</code>.</p>",
  },
  {
    question: "Какое свойство включает flex-раскладку?",
    answer: "flex",
    options: [
      { value: "flex", label: "`display: flex`", explain: "Верно. Это включает flex-раскладку." },
      { value: "padding", label: "`padding`", explain: "Нет. `padding` задает внутренние отступы." },
      { value: "color", label: "`color`", explain: "Нет. `color` меняет цвет текста." },
      { value: "font", label: "`font-family`", explain: "Нет. Это выбор шрифта." },
      { value: "border", label: "`border`", explain: "Нет. Это рамка элемента." },
    ],
    body: "<p><code>display: flex</code> помогает выравнивать элементы в строку или колонку.</p>",
  },
  {
    question: "Какой атрибут у картинки задает путь к файлу?",
    answer: "src",
    options: [
      { value: "src", label: "`src`", explain: "Верно. В `src` указывают путь к изображению." },
      { value: "href", label: "`href`", explain: "Нет. `href` используют у ссылок." },
      { value: "alt", label: "`alt`", explain: "Нет. `alt` задает текстовое описание картинки." },
      { value: "class", label: "`class`", explain: "Нет. `class` нужен для CSS и JS." },
      { value: "title", label: "`title`", explain: "Нет. `title` может дать подсказку, но не путь к файлу." },
    ],
    body: "<p>У изображения путь пишется так: <code>&lt;img src=\"...\" alt=\"...\"&gt;</code>.</p>",
  },
  {
    question: "Какое свойство CSS задает внешний отступ?",
    answer: "margin",
    options: [
      { value: "margin", label: "`margin`", explain: "Верно. `margin` задает внешний отступ." },
      { value: "padding", label: "`padding`", explain: "Нет. `padding` задает внутренний отступ." },
      { value: "gap", label: "`gap`", explain: "Нет. `gap` задает расстояние между элементами в flex/grid." },
      { value: "width", label: "`width`", explain: "Нет. Это ширина элемента." },
      { value: "height", label: "`height`", explain: "Нет. Это высота элемента." },
    ],
    body: "<p>Если нужно отодвинуть элемент от соседей снаружи, используется <code>margin</code>.</p>",
  },
  {
    question: "Какой тег подключает JavaScript-файл?",
    answer: "script",
    options: [
      { value: "script", label: "`<script>`", explain: "Верно. JS подключают тегом `<script>`." },
      { value: "link", label: "`<link>`", explain: "Нет. `<link>` обычно подключает CSS." },
      { value: "style", label: "`<style>`", explain: "Нет. `<style>` нужен для CSS внутри HTML." },
      { value: "meta", label: "`<meta>`", explain: "Нет. `<meta>` хранит служебную информацию." },
      { value: "pre", label: "`<pre>`", explain: "Нет. `<pre>` отображает форматированный текст." },
    ],
    body: "<p>Внешний JavaScript подключают через <code>&lt;script src=\"...\"&gt;&lt;/script&gt;</code>.</p>",
  },
  {
    question: "Что делает `document.querySelector('.box')`?",
    answer: "find",
    options: [
      { value: "find", label: "Находит первый элемент `.box`", explain: "Верно. Метод возвращает первый подходящий элемент." },
      { value: "delete", label: "Удаляет все элементы `.box`", explain: "Нет. Он только ищет элемент." },
      { value: "style", label: "Создает CSS-файл", explain: "Нет. CSS-файл он не создает." },
      { value: "reload", label: "Перезагружает страницу", explain: "Нет. Для перезагрузки есть другие методы." },
      { value: "image", label: "Сохраняет картинку", explain: "Нет. Это метод поиска элемента." },
    ],
    body: "<p><code>querySelector</code> нужен, чтобы получить элемент со страницы и потом работать с ним в JS.</p>",
  },
];

const stepDistractorPools = [
  [
    { value: "old-title", label: "Оставить `Document` в заголовке вкладки", explain: "Нет. Название вкладки должно объяснять проект." },
    { value: "broken-font", label: "Оставить неверную ссылку на шрифт", explain: "Нет. Неверный адрес не подключит нужные шрифты." },
    { value: "empty-alt", label: "Оставить пустой `alt` у логотипа", explain: "Нет. У логотипа лучше сделать понятное описание." },
  ],
  [
    { value: "pre-first", label: "Начать страницу сразу с длинного `pre`", explain: "Нет. Сначала нужен понятный первый экран." },
    { value: "tiny-button", label: "Оставить только маленькую кнопку в шапке", explain: "Нет. Нужна большая главная кнопка." },
    { value: "no-heading", label: "Не добавлять `h1`", explain: "Нет. Главный заголовок нужен для смысла страницы." },
  ],
  [
    { value: "replace-pre", label: "Заменить `pre` на набор `p`", explain: "Нет. Нужно сохранить форматированный текст." },
    { value: "hide-pre", label: "Спрятать `pre` через `display: none`", explain: "Нет. Блок должен быть видимым." },
    { value: "no-scroll", label: "Запретить прокрутку у широкого `pre`", explain: "Нет. Тогда блок может сломать мобильную ширину." },
  ],
  [
    { value: "random-images", label: "Добавить картинки без связи с редактором", explain: "Нет. Скриншоты должны показывать именно проект." },
    { value: "no-captions", label: "Поставить изображения без подписей", explain: "Нет. Подписи помогают понять, что на скриншоте." },
    { value: "no-alt", label: "Не писать `alt` у скриншотов", explain: "Нет. Описание изображения обязательно полезно." },
  ],
  [
    { value: "long-theory", label: "Написать длинную теорию вместо коротких карточек", explain: "Нет. Здесь нужны короткие объяснения." },
    { value: "only-design", label: "Рассказывать только про цвета сайта", explain: "Нет. Нужно объяснить работу редактора." },
    { value: "no-steps", label: "Не делить принцип работы на шаги", explain: "Нет. Разделение на шаги делает блок понятнее." },
  ],
  [
    { value: "random-css", label: "Добавлять стили случайно в конец файла", explain: "Нет. Лучше двигаться блоками: база, шапка, секции." },
    { value: "fixed-all", label: "Задать всем блокам фиксированную высоту", explain: "Нет. Так страница будет плохо адаптироваться." },
    { value: "remove-style", label: "Убрать зеленый цвет и черные рамки", explain: "Нет. Общий стиль проекта нужно сохранить." },
  ],
  [
    { value: "desktop-only", label: "Проверять только широкий экран", explain: "Нет. Нужна проверка и мобильной ширины." },
    { value: "hide-cta", label: "Спрятать кнопку рисования на телефоне", explain: "Нет. Главная кнопка должна оставаться доступной." },
    { value: "page-scroll-x", label: "Оставить горизонтальный скролл у всей страницы", explain: "Нет. Это выглядит как ошибка верстки." },
  ],
];

const completedStorageKey = "main-page-guide-completed";
const solvedStorageKey = "main-page-guide-solved";
const penaltySolvedStorageKey = "main-page-guide-penalty-solved";
const wrongCountsStorageKey = "main-page-guide-wrong-counts";
const stepWrongCountsStorageKey = "main-page-guide-step-wrong-counts";
const lockUntilStorageKey = "main-page-guide-lock-until";

const stepsRoot = document.querySelector("#steps");
const navRoot = document.querySelector("#stepNav");
const progressText = document.querySelector("#progressText");
const progressBar = document.querySelector("#progressBar");
const resetProgressButton = document.querySelector("#resetProgress");

const completedSteps = new Set(loadStoredArray(completedStorageKey));
const solvedHints = new Set(loadStoredArray(solvedStorageKey));
const solvedPenaltyQuestions = new Set(loadStoredArray(penaltySolvedStorageKey));
const wrongCounts = loadStoredObject(wrongCountsStorageKey);
const stepWrongCounts = loadStoredObject(stepWrongCountsStorageKey);
const lockUntilByQuestion = loadStoredObject(lockUntilStorageKey);
const activeCountdowns = new Map();

ensureOptionDepth();

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

function loadStoredArray(key) {
  try {
    const saved = JSON.parse(localStorage.getItem(key) || "[]");
    return Array.isArray(saved) ? saved : [];
  } catch {
    return [];
  }
}

function loadStoredObject(key) {
  try {
    const saved = JSON.parse(localStorage.getItem(key) || "{}");
    return saved && typeof saved === "object" ? saved : {};
  } catch {
    return {};
  }
}

function saveStoredSet(key, valueSet) {
  try {
    localStorage.setItem(key, JSON.stringify([...valueSet]));
  } catch {
    /* State still works until reload. */
  }
}

function saveStoredObject(key, value) {
  try {
    localStorage.setItem(key, JSON.stringify(value));
  } catch {
    /* State still works until reload. */
  }
}

function formatInlineCode(value) {
  return escapeHtml(value).replace(/`([^`]+)`/g, "<code>$1</code>");
}

function pluralizeQuestions(count) {
  const mod10 = count % 10;
  const mod100 = count % 100;

  if (mod10 === 1 && mod100 !== 11) return "вопрос";
  if (mod10 >= 2 && mod10 <= 4 && (mod100 < 12 || mod100 > 14)) return "вопроса";
  return "вопросов";
}

function pluralizeSeconds(count) {
  const mod10 = count % 10;
  const mod100 = count % 100;

  if (mod10 === 1 && mod100 !== 11) return "секунда";
  if (mod10 >= 2 && mod10 <= 4 && (mod100 < 12 || mod100 > 14)) return "секунды";
  return "секунд";
}

function pluralizePenaltyQuestions(count) {
  const mod10 = count % 10;
  const mod100 = count % 100;

  if (mod10 === 1 && mod100 !== 11) return "штрафной вопрос";
  if (mod10 >= 2 && mod10 <= 4 && (mod100 < 12 || mod100 > 14)) return "штрафных вопроса";
  return "штрафных вопросов";
}

function getStepId(index) {
  return `main-page-step-${index + 1}`;
}

function getHintId(stepIndex, hintIndex) {
  return `${getStepId(stepIndex)}-hint-${hintIndex + 1}`;
}

function getPenaltyId(stepIndex, penaltyIndex) {
  return `${getStepId(stepIndex)}-penalty-${penaltyIndex + 1}`;
}

function getSolvedCount(stepIndex) {
  return steps[stepIndex].hints.filter((_, hintIndex) =>
    solvedHints.has(getHintId(stepIndex, hintIndex)),
  ).length;
}

function getStepWrongCount(stepIndex) {
  return Number(stepWrongCounts[getStepId(stepIndex)] || 0);
}

function getPenaltyCount(stepIndex) {
  return Math.floor(getStepWrongCount(stepIndex) / 3);
}

function getSolvedPenaltyCount(stepIndex) {
  const penaltyCount = getPenaltyCount(stepIndex);
  let solved = 0;

  for (let penaltyIndex = 0; penaltyIndex < penaltyCount; penaltyIndex++) {
    if (solvedPenaltyQuestions.has(getPenaltyId(stepIndex, penaltyIndex))) {
      solved += 1;
    }
  }

  return solved;
}

function getTotalQuestionCount(stepIndex) {
  return steps[stepIndex].hints.length + getPenaltyCount(stepIndex);
}

function getTotalSolvedCount(stepIndex) {
  return getSolvedCount(stepIndex) + getSolvedPenaltyCount(stepIndex);
}

function isStepFullySolved(stepIndex) {
  return (
    getSolvedCount(stepIndex) === steps[stepIndex].hints.length &&
    getSolvedPenaltyCount(stepIndex) === getPenaltyCount(stepIndex)
  );
}

function getPenaltyQuestion(stepIndex, penaltyIndex) {
  return penaltyQuestionBank[(stepIndex * 3 + penaltyIndex) % penaltyQuestionBank.length];
}

function getQuestionWrongCount(questionId) {
  return Number(wrongCounts[questionId] || 0);
}

function incrementQuestionWrongCount(questionId) {
  wrongCounts[questionId] = getQuestionWrongCount(questionId) + 1;
  saveStoredObject(wrongCountsStorageKey, wrongCounts);
  return wrongCounts[questionId];
}

function incrementStepWrongCount(stepIndex) {
  const stepId = getStepId(stepIndex);
  stepWrongCounts[stepId] = Number(stepWrongCounts[stepId] || 0) + 1;
  saveStoredObject(stepWrongCountsStorageKey, stepWrongCounts);
  return Number(stepWrongCounts[stepId]);
}

function getLockDurationSeconds(wrongCount) {
  if (wrongCount <= 1) return 5;
  if (wrongCount === 2) return 15;
  return 30;
}

function getSecondsLeft(lockUntil) {
  return Math.max(0, Math.ceil((lockUntil - Date.now()) / 1000));
}

function getValidationMessage(stepIndex) {
  const total = getTotalQuestionCount(stepIndex);
  const solvedCount = getTotalSolvedCount(stepIndex);
  const remaining = total - solvedCount;
  const penaltyCount = getPenaltyCount(stepIndex);
  const remainingPenalty = penaltyCount - getSolvedPenaltyCount(stepIndex);

  if (remaining === total) {
    return `Ты отметил шаг как выполненный. Теперь ответь на все ${total} ${pluralizeQuestions(total)} этого шага, чтобы проверить себя. После верных ответов откроется итоговый код.`;
  }

  if (remainingPenalty > 0) {
    return `Шаг уже отмечен. Осталось ответить еще на ${remaining} из ${total} ${pluralizeQuestions(total)}. Среди них ${remainingPenalty} ${pluralizePenaltyQuestions(remainingPenalty)} за неправильные ответы.`;
  }

  return `Шаг уже отмечен. Осталось ответить еще на ${remaining} из ${total} ${pluralizeQuestions(total)}, и тогда откроется итоговый код этого шага.`;
}

function getStepStatusText(stepIndex, isDone, allSolved) {
  const penaltyCount = getPenaltyCount(stepIndex);

  if (isDone && !allSolved) {
    if (penaltyCount > 0) {
      return "Шаг отмечен. Сначала ответь на основные вопросы и дополнительные штрафные вопросы, чтобы открыть итоговый код.";
    }

    return "Шаг отмечен. Теперь ответь на все вопросы этого шага, чтобы проверить себя и открыть итоговый код.";
  }

  if (isDone && allSolved) {
    return "Шаг выполнен и проверен. Итоговый код уже открыт ниже.";
  }

  if (!isDone && allSolved) {
    return "Все вопросы уже пройдены. Теперь можешь смело отмечать шаг выполненным.";
  }

  return "Сначала попробуй сделать правку сам, а если застрянешь, открывай подсказки через вопросы.";
}

function ensureOptionDepth() {
  steps.forEach((step, stepIndex) => {
    const distractors = stepDistractorPools[stepIndex] || [];

    step.hints.forEach((hint, hintIndex) => {
      const enrichedOptions = [...hint.options];

      for (
        let extraIndex = 0;
        enrichedOptions.length < 5 && extraIndex < distractors.length;
        extraIndex++
      ) {
        const distractor = distractors[extraIndex];
        const nextValue = `${distractor.value}-${stepIndex}-${hintIndex}-${extraIndex}`;

        enrichedOptions.push({
          value: nextValue,
          label: distractor.label,
          explain: distractor.explain,
        });
      }

      hint.options = enrichedOptions;
    });
  });
}

function shuffleOptions(options) {
  const copy = [...options];

  for (let index = copy.length - 1; index > 0; index--) {
    const randomIndex = Math.floor(Math.random() * (index + 1));
    const current = copy[index];
    copy[index] = copy[randomIndex];
    copy[randomIndex] = current;
  }

  return copy;
}

function render() {
  navRoot.innerHTML = steps
    .map((step, index) => {
      const id = getStepId(index);
      return `
        <a href="#${id}" class="${completedSteps.has(id) ? "is-done" : ""}">
          <span class="route__num">${index + 1}</span>
          <span class="route__title">${escapeHtml(step.title)}</span>
        </a>
      `;
    })
    .join("");

  stepsRoot.innerHTML = steps.map((step, index) => renderStep(step, index)).join("");
  updateProgress();
  updateSolvedState();
  restoreActiveLocks();
}

function renderStep(step, index) {
  const stepId = getStepId(index);
  const solvedCount = getSolvedCount(index);
  const solvedPenaltyCount = getSolvedPenaltyCount(index);
  const penaltyCount = getPenaltyCount(index);
  const totalSolved = solvedCount + solvedPenaltyCount;
  const totalQuestions = getTotalQuestionCount(index);
  const allSolved = isStepFullySolved(index);
  const isDone = completedSteps.has(stepId);
  const needsValidation = isDone && !allSolved;

  return `
    <article class="step" id="${stepId}" data-step="${stepId}" data-step-index="${index}">
      <header class="step__header">
        <span class="step__number">${index + 1}</span>
        <div>
          <h2>${escapeHtml(step.title)}</h2>
          <p class="step__goal">${formatInlineCode(step.goal)}</p>
        </div>
      </header>

      <div class="flow">
        <section class="flow__item">
          <h3>Что было</h3>
          <p>${formatInlineCode(step.before)}</p>
        </section>
        <section class="flow__item">
          <h3>Что изменить</h3>
          <p>${formatInlineCode(step.change)}</p>
        </section>
        <section class="flow__item">
          <h3>Зачем</h3>
          <p>${formatInlineCode(step.why)}</p>
        </section>
      </div>

      <div class="task">
        <p><strong>Твоя задача:</strong> ${formatInlineCode(step.task)}</p>
      </div>

      <section class="step__checkin" data-step-checkin="${stepId}" ${needsValidation ? "" : "hidden"}>
        <span class="hint__level">Проверь себя</span>
        <p>${needsValidation ? getValidationMessage(index) : ""}</p>
      </section>

      <div class="practice">
        <p>Для закрепления ответь на все ${totalQuestions} ${pluralizeQuestions(totalQuestions)} этого шага. После правильных ответов откроется итоговый код.</p>
        ${penaltyCount > 0 ? `<p class="practice__penalty">Из-за перебора ответов здесь добавлено ${penaltyCount} ${pluralizePenaltyQuestions(penaltyCount)}.</p>` : ""}
        <p class="practice__count" data-step-progress="${stepId}">${totalSolved} / ${totalQuestions} вопросов</p>
      </div>

      <div class="hints">
        ${step.hints.map((hint, hintIndex) => renderHint(hint, index, hintIndex)).join("")}
      </div>

      ${penaltyCount > 0 ? renderPenaltyBlock(index, penaltyCount) : ""}

      <section class="step__result" data-step-result="${stepId}" ${allSolved ? "" : "hidden"}>
        <div class="step__result-head">
          <span class="hint__level">Итоговый код шага</span>
          <p>${formatInlineCode(step.result.location)}</p>
        </div>
        <div class="result-grid">
          <section class="result-card">
            <h3>Что было</h3>
            <pre><code>${escapeHtml(step.result.before)}</code></pre>
          </section>
          <section class="result-card">
            <h3>Что должно получиться</h3>
            <pre><code>${escapeHtml(step.result.after)}</code></pre>
          </section>
        </div>
        <p class="step__result-note">${formatInlineCode(step.result.note)}</p>
      </section>

      <footer class="step__footer">
        <div class="step__footer-copy">
          <p class="step__status">${getStepStatusText(index, isDone, allSolved)}</p>
          <div class="step__recap" data-step-recap="${stepId}" ${isDone ? "" : "hidden"}>
            <span class="hint__level">Коротко по шагу</span>
            <p>${formatInlineCode(step.recap)}</p>
          </div>
        </div>
        <button class="button button--ghost complete-step" type="button" data-complete="${stepId}">
          ${isDone ? "Снять отметку" : "Отметить шаг выполненным"}
        </button>
      </footer>
    </article>
  `;
}

function renderHint(hint, stepIndex, hintIndex) {
  const hintId = getHintId(stepIndex, hintIndex);
  const isSolved = solvedHints.has(hintId);
  const options = shuffleOptions(hint.options);

  return `
    <section class="hint ${isSolved ? "is-solved" : ""}" data-hint="${hintId}">
      <div class="hint__top">
        <h3>${escapeHtml(hint.level)}</h3>
        <span class="hint__difficulty">${escapeHtml(hint.difficulty)}</span>
      </div>

      <form
        class="hint__form ${isSolved ? "is-solved-form" : ""}"
        data-answer="${escapeHtml(hint.answer)}"
        data-question-id="${hintId}"
        data-step-index="${stepIndex}"
        data-penalty="false"
      >
        <p class="question">${formatInlineCode(hint.question)}</p>
        <div class="options">
          ${options
            .map((option) => {
              const optionId = `${hintId}-${option.value}`;
              return `
                <label class="option" for="${optionId}">
                  <input
                    id="${optionId}"
                    type="radio"
                    name="${hintId}"
                    value="${escapeHtml(option.value)}"
                    data-explain="${escapeHtml(option.explain)}"
                    ${isSolved ? "disabled" : ""}
                  />
                  <span>${formatInlineCode(option.label)}</span>
                </label>
              `;
            })
            .join("")}
        </div>
        <div class="hint__actions">
          <button class="button" type="submit" ${isSolved ? "disabled" : ""}>Проверить и открыть</button>
          <p class="feedback ${isSolved ? "is-ok" : ""}" aria-live="polite">${isSolved ? "Верный ответ уже принят. Подсказка открыта." : ""}</p>
        </div>
      </form>

      <div class="hint__body" ${isSolved ? "" : "hidden"}>
        <span class="hint__level">${escapeHtml(hint.level)} открыта</span>
        ${hint.body}
      </div>
    </section>
  `;
}

function renderPenaltyBlock(stepIndex, penaltyCount) {
  return `
    <section class="penalty-block" data-penalty-step="${getStepId(stepIndex)}">
      <div class="penalty-block__head">
        <span class="hint__level">Дополнительные вопросы</span>
        <p>За неправильные ответы здесь появилось ${penaltyCount} ${pluralizePenaltyQuestions(penaltyCount)}. Они простые, но пройти их нужно до открытия итогового кода.</p>
      </div>
      <div class="hints">
        ${Array.from({ length: penaltyCount }, (_, penaltyIndex) => renderPenaltyQuestion(stepIndex, penaltyIndex)).join("")}
      </div>
    </section>
  `;
}

function renderPenaltyQuestion(stepIndex, penaltyIndex) {
  const penaltyId = getPenaltyId(stepIndex, penaltyIndex);
  const question = getPenaltyQuestion(stepIndex, penaltyIndex);
  const isSolved = solvedPenaltyQuestions.has(penaltyId);
  const options = shuffleOptions(question.options);

  return `
    <section class="hint penalty ${isSolved ? "is-solved" : ""}" data-penalty="${penaltyId}">
      <div class="hint__top">
        <h3>Штрафной вопрос ${penaltyIndex + 1}</h3>
        <span class="hint__difficulty">Базовый вопрос</span>
      </div>

      <form
        class="hint__form penalty__form ${isSolved ? "is-solved-form" : ""}"
        data-answer="${escapeHtml(question.answer)}"
        data-question-id="${penaltyId}"
        data-step-index="${stepIndex}"
        data-penalty="true"
      >
        <p class="question">${formatInlineCode(question.question)}</p>
        <div class="options">
          ${options
            .map((option) => {
              const optionId = `${penaltyId}-${option.value}`;
              return `
                <label class="option" for="${optionId}">
                  <input
                    id="${optionId}"
                    type="radio"
                    name="${penaltyId}"
                    value="${escapeHtml(option.value)}"
                    data-explain="${escapeHtml(option.explain)}"
                    ${isSolved ? "disabled" : ""}
                  />
                  <span>${formatInlineCode(option.label)}</span>
                </label>
              `;
            })
            .join("")}
        </div>
        <div class="hint__actions">
          <button class="button" type="submit" ${isSolved ? "disabled" : ""}>Проверить ответ</button>
          <p class="feedback ${isSolved ? "is-ok" : ""}" aria-live="polite">${isSolved ? "Верный ответ уже принят." : ""}</p>
        </div>
      </form>

      <div class="hint__body" ${isSolved ? "" : "hidden"}>
        <span class="hint__level">Короткое пояснение</span>
        ${question.body}
      </div>
    </section>
  `;
}

function updateProgress() {
  const total = steps.length;
  const done = [...completedSteps].filter((id) => id.startsWith("main-page-step-")).length;
  progressText.textContent = `${done} из ${total} шагов`;
  progressBar.style.width = `${(done / total) * 100}%`;
}

function updateSolvedState() {
  steps.forEach((step, stepIndex) => {
    const stepId = getStepId(stepIndex);
    const totalSolved = getTotalSolvedCount(stepIndex);
    const totalQuestions = getTotalQuestionCount(stepIndex);
    const allSolved = isStepFullySolved(stepIndex);
    const isDone = completedSteps.has(stepId);

    const counter = document.querySelector(`[data-step-progress="${stepId}"]`);
    const result = document.querySelector(`[data-step-result="${stepId}"]`);
    const recap = document.querySelector(`[data-step-recap="${stepId}"]`);
    const checkin = document.querySelector(`[data-step-checkin="${stepId}"]`);
    const stepElement = document.querySelector(`[data-step="${stepId}"]`);
    const status = stepElement?.querySelector(".step__status");
    const button = stepElement?.querySelector(".complete-step");

    if (counter) {
      counter.textContent = allSolved
        ? `${totalSolved} / ${totalQuestions} вопросов. Итоговый код открыт.`
        : `${totalSolved} / ${totalQuestions} вопросов`;
    }

    if (result) {
      result.hidden = !allSolved;
    }

    if (recap) {
      recap.hidden = !isDone;
    }

    if (checkin) {
      checkin.hidden = !(isDone && !allSolved);
      if (isDone && !allSolved) {
        checkin.querySelector("p").textContent = getValidationMessage(stepIndex);
      }
    }

    if (status) {
      status.textContent = getStepStatusText(stepIndex, isDone, allSolved);
    }

    if (button) {
      button.textContent = isDone ? "Снять отметку" : "Отметить шаг выполненным";
    }
  });

  document.querySelectorAll("#stepNav a").forEach((link, index) => {
    link.classList.toggle("is-done", completedSteps.has(getStepId(index)));
  });
}

function disableQuestionForm(form, disabled) {
  form.classList.toggle("is-locked", disabled);
  form.querySelectorAll("input, button").forEach((element) => {
    element.disabled = disabled || form.classList.contains("is-solved-form");
  });
}

function clearCountdown(questionId) {
  const timerId = activeCountdowns.get(questionId);

  if (timerId) {
    window.clearInterval(timerId);
    activeCountdowns.delete(questionId);
  }
}

function clearAllCountdowns() {
  activeCountdowns.forEach((timerId) => {
    window.clearInterval(timerId);
  });
  activeCountdowns.clear();
}

function showLockMessage(form, secondsLeft) {
  const feedback = form.querySelector(".feedback");
  feedback.classList.remove("is-ok", "is-error");
  feedback.classList.add("is-locked");
  feedback.textContent = `Вариант неправильный, внимательно прочитай задание и попробуй снова. Осталось ${secondsLeft} ${pluralizeSeconds(secondsLeft)}.`;
}

function startCountdown(form, questionId, lockUntil) {
  clearCountdown(questionId);
  disableQuestionForm(form, true);
  showLockMessage(form, getSecondsLeft(lockUntil));

  const timerId = window.setInterval(() => {
    const secondsLeft = getSecondsLeft(lockUntil);

    if (secondsLeft <= 0) {
      window.clearInterval(timerId);
      activeCountdowns.delete(questionId);
      delete lockUntilByQuestion[questionId];
      saveStoredObject(lockUntilStorageKey, lockUntilByQuestion);

      if (!form.classList.contains("is-solved-form")) {
        disableQuestionForm(form, false);
        const feedback = form.querySelector(".feedback");
        feedback.classList.remove("is-locked");
        feedback.classList.add("is-error");
        feedback.textContent = "Можно попробовать снова.";
      }

      return;
    }

    showLockMessage(form, secondsLeft);
  }, 1000);

  activeCountdowns.set(questionId, timerId);
}

function restoreActiveLocks() {
  clearAllCountdowns();

  Object.entries(lockUntilByQuestion).forEach(([questionId, rawLockUntil]) => {
    const lockUntil = Number(rawLockUntil);

    if (!lockUntil || lockUntil <= Date.now()) {
      delete lockUntilByQuestion[questionId];
      return;
    }

    const form = document.querySelector(`[data-question-id="${questionId}"]`);

    if (form && !form.classList.contains("is-solved-form")) {
      startCountdown(form, questionId, lockUntil);
    }
  });

  saveStoredObject(lockUntilStorageKey, lockUntilByQuestion);
}

document.addEventListener("submit", (event) => {
  const form = event.target.closest(".hint__form");
  if (!form) return;

  event.preventDefault();

  if (form.classList.contains("is-solved-form")) {
    return;
  }

  const questionId = form.dataset.questionId;
  const stepIndex = Number(form.dataset.stepIndex);
  const isPenalty = form.dataset.penalty === "true";
  const lockUntil = Number(lockUntilByQuestion[questionId] || 0);

  if (lockUntil > Date.now()) {
    startCountdown(form, questionId, lockUntil);
    return;
  }

  const selected = form.querySelector("input[type='radio']:checked");
  const feedback = form.querySelector(".feedback");
  const hintBody = form.nextElementSibling;

  feedback.classList.remove("is-ok", "is-error", "is-locked");

  if (!selected) {
    feedback.textContent = "Сначала выбери один вариант ответа.";
    feedback.classList.add("is-error");
    return;
  }

  const explanation = selected.dataset.explain;

  if (selected.value !== form.dataset.answer) {
    const currentWrongCount = incrementQuestionWrongCount(questionId);
    const durationSeconds = getLockDurationSeconds(currentWrongCount);
    const nextLockUntil = Date.now() + durationSeconds * 1000;
    lockUntilByQuestion[questionId] = nextLockUntil;
    saveStoredObject(lockUntilStorageKey, lockUntilByQuestion);

    if (!isPenalty) {
      const previousPenaltyCount = getPenaltyCount(stepIndex);
      incrementStepWrongCount(stepIndex);
      const nextPenaltyCount = getPenaltyCount(stepIndex);

      if (nextPenaltyCount !== previousPenaltyCount) {
        render();
        return;
      }
    }

    startCountdown(form, questionId, nextLockUntil);
    updateSolvedState();
    return;
  }

  feedback.textContent = explanation;
  feedback.classList.add("is-ok");
  hintBody.hidden = false;
  form.classList.add("is-solved-form");
  disableQuestionForm(form, true);
  clearCountdown(questionId);
  delete lockUntilByQuestion[questionId];
  saveStoredObject(lockUntilStorageKey, lockUntilByQuestion);

  if (isPenalty) {
    if (!solvedPenaltyQuestions.has(questionId)) {
      solvedPenaltyQuestions.add(questionId);
      saveStoredSet(penaltySolvedStorageKey, solvedPenaltyQuestions);
    }
  } else if (!solvedHints.has(questionId)) {
    solvedHints.add(questionId);
    saveStoredSet(solvedStorageKey, solvedHints);
  }

  updateSolvedState();
});

document.addEventListener("click", (event) => {
  const button = event.target.closest(".complete-step");
  if (!button) return;

  const stepId = button.dataset.complete;
  const stepElement = document.querySelector(`[data-step="${stepId}"]`);
  const stepIndex = Number(stepElement.dataset.stepIndex);

  if (completedSteps.has(stepId)) {
    completedSteps.delete(stepId);
  } else {
    completedSteps.add(stepId);
  }

  saveStoredSet(completedStorageKey, completedSteps);
  updateProgress();
  updateSolvedState();

  const isDone = completedSteps.has(stepId);
  const allSolved = isStepFullySolved(stepIndex);

  if (isDone && !allSolved) {
    const checkin = stepElement.querySelector("[data-step-checkin]");
    const practice = stepElement.querySelector(".practice");

    checkin.hidden = false;
    checkin.classList.add("is-highlighted");
    practice.classList.add("is-highlighted");
    checkin.scrollIntoView({ behavior: "smooth", block: "center" });

    window.setTimeout(() => {
      checkin.classList.remove("is-highlighted");
      practice.classList.remove("is-highlighted");
    }, 1500);
  }
});

resetProgressButton?.addEventListener("click", () => {
  const shouldReset = window.confirm(
    "Сбросить все ответы, отметки шагов, штрафные вопросы и открытые подсказки?",
  );

  if (!shouldReset) return;

  completedSteps.clear();
  solvedHints.clear();
  solvedPenaltyQuestions.clear();
  clearAllCountdowns();

  Object.keys(wrongCounts).forEach((key) => delete wrongCounts[key]);
  Object.keys(stepWrongCounts).forEach((key) => delete stepWrongCounts[key]);
  Object.keys(lockUntilByQuestion).forEach((key) => delete lockUntilByQuestion[key]);

  saveStoredSet(completedStorageKey, completedSteps);
  saveStoredSet(solvedStorageKey, solvedHints);
  saveStoredSet(penaltySolvedStorageKey, solvedPenaltyQuestions);
  saveStoredObject(wrongCountsStorageKey, wrongCounts);
  saveStoredObject(stepWrongCountsStorageKey, stepWrongCounts);
  saveStoredObject(lockUntilStorageKey, lockUntilByQuestion);

  render();
});

render();
